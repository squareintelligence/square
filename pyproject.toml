[project]
name = "square"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.39.0",
    "botocore[crt]",
    "joblib>=1.5.0",
    "numpy>=2.4.4",
    "scikit-learn"
]

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
